Screenshots referenced by `shots` in data/projects.json.

See ../../../notes/SHOTLIST.md for the capture list and the redaction checklist.
Nothing here is required — a project with no shots renders no gallery at all.
